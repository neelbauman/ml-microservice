# Dev overrides — auto-loaded by docker compose.
# Switches to dev target (editable install + reload) and mounts source.
services:
  ingestion:
    build:
      target: dev
    volumes:
      - ./source/libs/common/src:/app/source/libs/common/src
      - ./source/services/ingestion/src:/app/source/services/ingestion/src

  preprocessing:
    build:
      target: dev
    volumes:
      - ./source/libs/common/src:/app/source/libs/common/src
      - ./source/services/preprocessing/src:/app/source/services/preprocessing/src

  inference:
    build:
      target: dev
    volumes:
      - ./source/libs/common/src:/app/source/libs/common/src
      - ./source/services/inference/src:/app/source/services/inference/src

  alert:
    build:
      target: dev
    volumes:
      - ./source/libs/common/src:/app/source/libs/common/src
      - ./source/services/alert/src:/app/source/services/alert/src

  dashboard:
    build:
      target: dev
    volumes:
      - ./source/libs/common/src:/app/source/libs/common/src
      - ./source/services/dashboard/src:/app/source/services/dashboard/src
