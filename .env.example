# ============================================================
# ML Pipeline — Local Development Environment Variables
# ============================================================
# Copy to .env and adjust as needed:
#   cp .env.example .env
#
# .env is auto-loaded by:
#   - docker compose (for container environment)
#   - Makefile training targets (for local script execution)
# ============================================================

# --- MinIO / S3 ---
AWS_ACCESS_KEY_ID=minioadmin
AWS_SECRET_ACCESS_KEY=minioadmin
MLFLOW_S3_ENDPOINT_URL=http://localhost:9000

# --- MLflow ---
MLFLOW_TRACKING_URI=http://localhost:5001
MLFLOW_EXPERIMENT_NAME=anomaly-detection

# --- PostgreSQL (MLflow backend) ---
POSTGRES_DB=mlflow
POSTGRES_USER=mlflow
POSTGRES_PASSWORD=mlflow

# --- MinIO ---
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin

# --- Dapr (host-side publish) ---
DAPR_HTTP_PORT=3501
